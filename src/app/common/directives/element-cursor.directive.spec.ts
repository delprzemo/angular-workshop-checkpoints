import { ElementCursorDirective } from './element-cursor.directive';

describe('ElementCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementCursorDirective();
    expect(directive).toBeTruthy();
  });
});
