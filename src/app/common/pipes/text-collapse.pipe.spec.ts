import { TextCollapsePipe } from './text-collapse.pipe';

describe('TextCollapsePipe', () => {
  it('create an instance', () => {
    const pipe = new TextCollapsePipe();
    expect(pipe).toBeTruthy();
  });
});
